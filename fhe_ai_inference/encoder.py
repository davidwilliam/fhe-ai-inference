# fhe_ai_inference/encoder.py


def encrypt_tensor(tensor, encoder=None, encryptor=None):
    # Bypass encryption for now
    return tensor


def decrypt_tensor(ciphertext, decoder=None, decryptor=None):
    # Bypass decryption for now
    return ciphertext
